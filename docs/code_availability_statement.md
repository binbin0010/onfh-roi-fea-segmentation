# Code Availability Statement Template

Replace the bracketed fields after the GitHub release and Zenodo archive are
created.

```text
Code availability
The scripts used for segmentation-assisted ROI initialization, mask refinement,
volume calculation, STL export, and finite-element preprocessing are available
at GitHub ([GitHub URL]) and have been archived in Zenodo ([DOI]). The archived
version v1.0.0 corresponds to the code used in this study. Patient-level DICOM
data are not publicly available because of privacy and ethical restrictions.
```
